---
const isActive = Astro.props.href === '/' ?
    Astro.routePattern === '/' :
    Astro.routePattern.startsWith(Astro.props.href);
---
<li>
    <a class={`relative block px-3 py-2 transition hover:text-accent ${isActive ? 'text-accent' : ''}`} href={Astro.props.href}><slot /></a>
</li>
